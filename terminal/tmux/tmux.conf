# ── Terminal & Compatibility ─────────────────────────────────────
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm-kitty:RGB"
set -as terminal-overrides ",xterm-256color:RGB"    # SSH client true color
set -g mouse on
set -sg escape-time 10                              # Low delay (0 breaks arrow keys over SSH)
set -g history-limit 200000                          # Generous scrollback
set -g focus-events on                               # For editor autoread
set -g set-clipboard on                              # System clipboard sync
set -g base-index 1                                  # Windows start at 1
setw -g pane-base-index 1                            # Panes start at 1
set -g renumber-windows on                           # No gaps after closing
set -g allow-passthrough on                          # Kitty graphics/shell integration
setw -g aggressive-resize on                         # Size per-client (local vs SSH)
set -g display-time 3000                             # Readable status messages
set -ga update-environment "COLORTERM KITTY_PID KITTY_WINDOW_ID KITTY_LISTEN_ON"

# Undercurl support (for tools that use styled underlines)
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# ── Prefix ───────────────────────────────────────────────────────
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# ── Key Bindings ─────────────────────────────────────────────────
setw -g mode-keys vi

# Intuitive splits (open in current directory)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
unbind '"'
unbind %

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing (repeatable with -r)
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Copy mode (vi + pbcopy)
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

# Reload config
bind r source-file ~/.config/tmux/tmux.conf \; display-message "Config reloaded"

# ── Appearance (Osaka Jade — exact colors from theme) ────────────────────
set -g status-position bottom
set -g status-justify left
set -g status-style "bg=#111c18,fg=#53685B"

# Session name with Powerline separator (gold on jade night)
set -g status-left "#[bg=#E5C736,fg=#111c18,bold] #S #[fg=#E5C736,bg=#111c18]"
set -g status-left-length 20

# Right: continuum save indicator + time
set -g status-right "#[fg=#549e6a]#{continuum_status} #[fg=#53685B]%H:%M "
set -g status-right-length 20

# Clickable window tabs with Powerline arrows
# Inactive: dark jade surface, dim text
setw -g window-status-format "#[fg=#23372B,bg=#111c18]#[fg=#53685B,bg=#23372B] #I:#W #[fg=#23372B,bg=#111c18]"
# Active: jade green surface, dark text (matches Kitty active tab)
setw -g window-status-current-format "#[fg=#509475,bg=#111c18]#[fg=#111c18,bg=#509475,bold] #I:#W #[fg=#509475,bg=#111c18]"
setw -g window-status-separator ""

# Pane borders
set -g pane-border-style "fg=#23372B"
set -g pane-active-border-style "fg=#509475"

# Messages
set -g message-style "bg=#23372B,fg=#C1C497"
set -g message-command-style "bg=#23372B,fg=#C1C497"

# Auto-rename windows to current directory basename
setw -g automatic-rename on
setw -g automatic-rename-format '#{b:pane_current_path}'

# Kitty title bar shows useful info (replaces tab title template)
set -g set-titles on
set -g set-titles-string "#S:#W — #{pane_current_path}"

# Reduce noise
setw -g monitor-activity off
set -g visual-activity off

# ── Plugins ──────────────────────────────────────────────────────
# Pin the install target explicitly so tpm never drifts between
# ~/.tmux/plugins and this XDG location (both existed once; XDG won).
set-environment -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.config/tmux/plugins/"
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'

# Session persistence
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '10'

# Thumbs — quick-copy URLs, paths, git SHAs (prefix+Space → hint letters)
set -g @thumbs-key Space
set -g @thumbs-command 'echo -n {} | pbcopy'
set -g @thumbs-fg-color '#E5C736'
set -g @thumbs-hint-fg-color '#2DD5B7'
set -g @thumbs-bg-color '#111c18'

# Initialize TPM (must be last line)
run '~/.config/tmux/plugins/tpm/tpm'
